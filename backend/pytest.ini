[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    nodb: pure unit test needing no database (skips Postgres schema setup/teardown)
    unit: fast unit test with no external I/O
    integration: test that exercises the DB or mocked external services
